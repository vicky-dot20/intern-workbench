# Validation

Run `npm run validate` from the repository root. It checks the framework contract, not whether product code is elegant. Each task must also define the closest meaningful check: test, typecheck, build, request, accessibility inspection, query plan, measurement, or reviewed design. Capture enough evidence for another person to reproduce the result.

