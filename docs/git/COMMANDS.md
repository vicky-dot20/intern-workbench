# Git Workflow Commands

Use `git status` and `git diff` before staging. Start from an updated default branch with `git pull --ff-only`, create a task branch with `git switch -c <branch>`, stage explicit paths, commit with an intent-focused message, and push with `git push -u origin HEAD`. Never force-push a shared branch or rewrite history without mentor approval.

