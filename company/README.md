# Company Layer

Keep organization-specific choices here so the framework can be updated independently. Public forks must contain only generic or explicitly publishable information. Replace the safe placeholders in `PROFILE.yml`, write standards in `STANDARDS.md`, choose a curriculum route, update CODEOWNERS and the security link, then run validation.

